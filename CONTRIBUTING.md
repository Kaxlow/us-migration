# Contributing

1. Follow the environment setup in `README.md`.
2. Keep reusable logic in `src/us_migration/` and exploration in `notebooks/`.
3. Document data provenance and methodology in `docs/`.
4. Add meaningful tests when introducing reusable analysis logic.
5. Review changes for credentials, data files, and notebook outputs before committing.

TODO: Define formatting, linting, test commands, and review conventions when the
implementation and tooling are established.
